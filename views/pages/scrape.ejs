<div class="max-w-4xl mx-auto">
    <h1 class="text-3xl font-bold text-white mb-6">Lấy dữ liệu Google Play</h1>

    <div id="scrape-form" class="bg-gray-800 p-6 rounded-lg shadow-xl space-y-6">

        <div id="alert-container">
            </div>

        <div>
            <label for="appIdsList" class="block text-sm font-medium text-gray-300 mb-2">
                Danh sách App ID (Mỗi ID một dòng)
            </label>
            <textarea id="appIdsList" name="appIdsList" rows="10"
                class="w-full p-3 bg-gray-900 border border-gray-700 rounded-md text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200"
                placeholder="com.google.android.gm
com.facebook.katana
com.mojang.minecraftpe"></textarea>
            <p class="text-xs text-gray-500 mt-1">Loại bỏ trùng lặp và dòng trống tự động.</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
                <label for="concurrency" class="block text-sm font-medium text-gray-300 mb-2">
                    Số luồng (Concurrency)
                </label>
                <input type="number" id="concurrency" name="concurrency"
                    class="w-full p-3 bg-gray-900 border border-gray-700 rounded-md text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200"
                    value="5" min="1" max="50">
                <p class="text-xs text-gray-500 mt-1">Chạy song song bao nhiêu app một lúc.</p>
            </div>
            <div>
                <label for="delay" class="block text-sm font-medium text-gray-300 mb-2">
                    Delay (miligiây)
                </label>
                <input type="number" id="delay" name="delay"
                    class="w-full p-3 bg-gray-900 border border-gray-700 rounded-md text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200"
                    value="1000" min="0">
                <p class="text-xs text-gray-500 mt-1">Nghỉ bao lâu (ms) giữa mỗi lượt lấy.</p>
            </div>
        </div>

        <div>
            <button id="startScrapeBtn"
                class="w-full flex justify-center items-center p-4 bg-blue-600 hover:bg-blue-700 text-white font-bold rounded-lg shadow-lg transition duration-200 ease-in-out
                       disabled:bg-gray-600 disabled:cursor-not-allowed">
                
                <svg id="loading-spinner" class="animate-spin -ml-1 mr-3 h-5 w-5 text-white hidden" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                  <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                  <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                </svg>
                
                <span id="btn-text">Bắt đầu lấy dữ liệu</span>
            </button>
        </div>
    </div>
</div>